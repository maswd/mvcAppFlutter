import 'package:flutter/material.dart';

class CartSceren extends StatelessWidget {
  const CartSceren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("cart Screen"),
      ),
    );
  }
}
